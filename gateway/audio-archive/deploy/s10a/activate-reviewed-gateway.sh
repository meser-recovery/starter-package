#!/usr/bin/env bash
set -Eeuo pipefail

printf '%s\n' 'activate-reviewed-gateway.sh is permanently disabled: the gateway-only Storage Access topology was superseded by the S10A unified-service release.' >&2
exit 64
