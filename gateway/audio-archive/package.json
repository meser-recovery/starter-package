{
  "name": "meser-audio-archive-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/*.mjs && node --check test/*.mjs && node --check tools/*.mjs",
    "test": "node --test --test-concurrency=1 test/*.test.mjs",
    "password-verifier": "node tools/create-password-verifier.mjs"
  }
}
