# S10 pre-change cleanup audit

Baseline: `origin/main` at `24dc631df3dbc9c674d5d357e33d6b96bdf9eee8`. The audit used tracked-file search, import/reference search, DOM/CSS hook comparison, history/blame for ambiguous files, and the existing gateway/frontend/browser suites. Historical evidence was excluded from cleanup decisions.

| Candidate | Runtime/import/DOM/test references | Decision | Safety proof and protected invariant |
| --- | --- | --- | --- |
| `gateway/audio-archive/src/config.mjs` rollout parser/property; committed Compose environment entry; README configuration entry | Consumed only by `app.mjs`, `domain.mjs`, two config/app tests, Compose, and current runtime docs | REPLACE | Project history was already the accepted production capability. Remove configuration branching and assert unconditional `/v1/config.speakerProjectHistory: 1` in `auth-app.test.mjs`; gateway suite protects startup bounds and secrets. An unrelated external environment entry is ignored by the new runtime. |
| `AudioArchiveDomain.speakerProjectHistoryEnabled`, constructor option, mode guard helper, and four conditional Speaker paths | References were limited to Speaker begin-final, history/state/continuation, draft save, and S09D fixtures | REPLACE | Make current behavior unconditional. `s09d-project-history.test.mjs` protects immutable saves/history/continuation/exact final lineage; `speaker-save.test.mjs` protects idempotency, conflict, recovery, deletion, and monotonic versions. |
| Frontend capability conditionals in `audio-project.mjs` and `source-session-archive.mjs` that selected draft-only saves or recipe v1 finals | Called by explicit Speaker project save/final flows and recovery/continuation flows | REPLACE | A central gateway assertion now runs before any new Speaker mutation. `tests/safety/s09d_project_history.test.mjs` proves missing/malformed/outdated capability produces no Speaker `PUT`/`POST`, keeps local File/Blob/project/result objects, leaves Announcement writes available, and permits safe reads. |
| Capability conditionals in `audio-archive.mjs` and history rendering | DOM consumers are the current Archive detail/history/result controls | REPLACE | Keep safe playback/download and recipe-v1 recovery links; replace silent hiding with a visible incompatibility message. Browser smoke protects detail controls, deep links, and responsive behavior. |
| `globalThis.meserAudioArchiveDrafts` in `source-session-archive.mjs` | No repository consumer, import, HTML hook, test reference, or documented public contract; introduced as an S08A stage hook | REMOVE | Production code already calls the gateway instance directly. Full static, Node, and browser suites protect actual draft load/save paths. |
| Root file `test` | Zero bytes; no references; history shows it was an accidental rename of the zero-byte `.nojekyll` file in commit `bed113d`; it is absent from the stable-path contract | REMOVE | `check_site.py` protects public URLs and GitHub Pages files. The real test trees are `tests/` and `gateway/audio-archive/test/`. |
| Tracked root/audio `.DS_Store` files and untracked evidence-directory `.DS_Store` | No code or content references; all are Finder metadata | REMOVE | Add repository-wide ignore rule. The S10 hygiene guard checks the tracked index. Historical evidence content is otherwise untouched. |
| Existing `speaker/v1` draft payload and Speaker recipe v1 validators/readers | Active schema validators, project-state schema, archive reconstruction, continuation compatibility, retained fixtures, and tests | PRESERVE | These are data compatibility boundaries, not rollout switches. `speaker-editor.test.mjs`, `speaker-save.test.mjs`, and S09D tests protect strict legacy reads while canonical write tests reject recipe v1 finals. |
| Announcement compatibility routes/identifiers, legacy draft URL normalization, durable storage paths and transaction identities | Active gateway routes, repository adapter, recovery fixtures, and tests | PRESERVE | Renaming/removal would break stored records. Announcement, GitHub adapter, recovery, integrity, lifecycle, auth, CORS, and CSRF tests remain unchanged. |
| Existing legacy audio viewer/data contract and vendored PDF.js legacy runtime | Public HTML references and stable site/browser tests | PRESERVE | User-visible compatibility surfaces unrelated to the Speaker rollout. Static site and browser suites remain their guards. |
| Audio Editor/Archive HTML hooks and audio stylesheet selectors | Hooks are consumed directly and through generated class names/state attributes across editor, archive, transport, timeline, dialogs, and responsive CSS | PRESERVE | No additional hook/selector had unambiguous zero reachability. Deleting on static-text absence alone would be unsafe. Browser smoke plus focused S09C/S09D runs protect 320/390/768/1280 layouts, keyboard/ARIA behavior, transport, regions, Follow/Fit/zoom, and archive disclosure. |
| Frontend module exports/imports and Speaker/transport/timeline helpers | Consumers include direct imports, dynamically installed callbacks, and focused Node/browser tests | PRESERVE | No further export/helper was proven dead without ambiguity. Existing focused Node tests cover transport, waveform, analysis queue, render cache, and project core. |
| Stage-named validation logs, screenshots, fixtures, and Closure evidence | Historical acceptance records only | PRESERVE | Evidence remains immutable even when names describe older stages or failed runs; it is not runtime dead code. |

No architecture, DSP, schema identity, public URL, meeting-generation file, production environment, or external archive was changed by this audit.
