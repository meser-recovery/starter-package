# S10A FIX requirement / evidence matrix

Status: corrective incident evidence after the separately authorized 2026-09-20 activation attempt of accepted source `1ff33848bf228d6212f4691804911ef3d0533a57`. That attempt replaced the two application containers, encountered a startup readiness race at the first public HTTPS probe, invoked rollback, and returned non-zero when the legacy-origin rollback probe timed out. The old exact image IDs were restored and the canonical public route subsequently recovered. This remains neither successful deployment evidence nor operational recovery readiness or proof of behavior on real iPhone/Android devices.

| Requirement | Implementation evidence | Automated / visual evidence | Status |
| --- | --- | --- | --- |
| Canonical same-origin boundary | `service/frontend/`, custom `Caddy.Dockerfile`, `Caddyfile`, private `gateway:8080`, relative `/v1` client | gateway/self-hosted tests, frontend inventory, focused browser suite and Caddy-backed Docker CI | PASS locally and in run `35395663602` |
| One server-side session | `__Host-meser_service_session`, signed opaque ID, bounded in-memory `SessionRegistry`, restart invalidation | auth/app tests cover attributes, tamper, unknown, expiry, eviction, retired-cookie rejection/clearing and logout revocation | Covered locally |
| Login requires replay | `/login` submits once and accepts success only after authenticated `GET /v1/session` returns CSRF | client/gateway tests plus focused browser request trace | Covered locally |
| Caddy protected delivery | allowlisted document navigation uses `/internal/document-auth` and a gateway-canonicalized return intent; assets/data retain fail-closed `/internal/auth-check`; no `handle_errors` dependency; `/internal/*` is publicly hidden | gateway return-intent tests, self-hosted topology tests and Caddy-backed Docker E2E | PASS in run `35395663602`: unauthenticated `303`, authenticated page/API, fail-closed assets/data and external internal-route denial |
| Production CSP / FFmpeg WASM | `script-src` adds only `'wasm-unsafe-eval'`; general `'unsafe-eval'` remains absent | Caddy-backed Chromium E2E reads the actual response CSP and completes real self-hosted FFmpeg/WASM processing | PASS under actual Caddy headers in run `35395663602` |
| Pages/public separation | root service paths are compatibility stubs; ordinary public content remains on Pages and service links use the canonical origin | `check_site.py`, Node safety tests and public Chromium smoke | Covered locally |
| Strict deep-link transfer | Archive forwards only `session`; Editor forwards only `session`, `workflow`, `projectRevision`, `speakerOutput`; duplicates, unknowns, fragments and malformed values are dropped | compatibility parser and site-contract assertions | Covered locally |
| No superseded auth | client verifier, sessionStorage authority, Storage Access state machine, bootstrap/bridge routes, dual-cookie and Archive login/connect UI removed | production-string/static scans and gateway/frontend tests | Covered locally |
| Common reconnect | shared `ServiceSessionController`; exact expiry message; 401 reconnect only, 403 stays an authorization error; auth-generation fencing | focused browser suite preserves a synthetic local `File` across expiry/re-login and distinguishes 403 | Covered in Chromium locally; all engines in CI |
| Editor service logout | `service-editor-session.mjs` restores current CSRF, revokes the shared server session, clears cookies and navigates to `/login`; `prepareSpeakerLogout` preserves save/discard/cancel and suppresses unload only after explicit approval | focused browser suite cancels once with session intact, then discards, logs out and proves `/v1/session` is 401 | PASS locally in Chromium/WebKit and in all-engine run `35395663602`; Caddy logout/revocation also PASS there |
| Canonical prepared source batch | strict manifest/part/track validation, authenticated full download, byte/hash verification, authoritative reread and atomic commit retained | existing client/unit tests remain green | Covered locally |
| Progress/cancel/stale/shared Files/read-only open | abort/generation fencing and previous-context preservation retained; Announcement/Speaker receive exact prepared `File[]` | existing Node/browser regressions and static safety checks remain in the suite | Covered locally/CI |
| Reproducible full-service release | canonical locale/timezone, commit-backed `git archive`, `gzip -n`, documented GNU/Linux/Git/gzip/Node baseline, digest-pinned images and exact image refs/IDs | CI packages twice in independent empty directories and requires byte equality plus identical SHA-256 | PASS in run `35395663602`; independently repeated locally on the identical merged tree |
| Guarded activation/rollback | two false gates; validated full-JSON container-state capture preserves exact container/image/status/optional-health identity; shared bounded readiness waits for exact gateway health, healthless/running Caddy, then two consecutive canonical public `.ok=true` responses; rollback also excludes candidate IDs and never uses legacy runtime origin | unit fixtures cover transient curl 35/HTTP 503, fatal TLS/status/malformed responses, monotonic deadline exhaustion, healthless Caddy, exact-image refusal and log redaction; Docker rehearsal delays public readiness before validating real gateway/Caddy rollback | Corrective repository evidence only; production gates remain `false`; no repeat activation was performed |
| Authoritative activation evidence | exact release images are loaded and ID-checked; smoke includes canonical redirect, login/replay, protected routes, read-only source part bytes/SHA and logout; accepted activation writes a deployment record only after a verified post-activation bundle created from the installed canonical runtime | script/static tests; both production gates remain `false` | Not established: the 2026-09-20 attempt failed at the first public health probe before authentication/source smoke, post-activation bundle, config/runtime installation and deployment record |
| Canonical runtime / configured backup | shared `runtime-env.sh` writes and validates exactly 11 non-secret keys for activation, post-activation recovery, installed weekly backup and restore; source SHA and both image refs must equal the release manifest; malformed, missing, duplicate, unknown or mismatched identities fail closed | synthetic activation-shaped create/decrypt and configured-backup success; six runtime schema/identity refusals; static binding tests | Synthetic evidence only; no real backup or operational-readiness claim |
| Encrypted recovery / clean restore | age X25519, exact three-secret allowlist, canonical 11-key non-secret `runtime.env`, exact image refs/IDs, mount guard, integrity manifest, disabled timer and retention tooling | create/decrypt refusal suite plus Docker clean-machine load/ID/container/health/Caddy E2E rehearsal in canonical PR CI; the real pre-cutover bundle was verified separately before the attempt | Post-activation bundle was not reached; no operational recovery-readiness claim |
| Responsive protected UI | landing, Archive and Editor exercised at 320/390/768/1280 with overflow checks | focused Chromium smoke | Covered locally |
| Mobile long-audio waveform | `speaker-waveform.mjs` retains 65,536 overview peaks while decoding sequential chunks capped at 4,096×100 RGBA (1,638,400 bytes); one input write per track, unique paths, immediate output deletion, per-track worker lifecycle, abort fencing and safe stage diagnostics | exact 3,747.648-second synthetic AAC-LC/M4A fixture; Node coverage for gapless planning, memory bound, load/read/write/exec/read/convert/cleanup failures, aborts, retry and three-track serialization; real bundled decoder in Chromium/Firefox/WebKit and Caddy-backed CSP rehearsal | Corrective implementation evidence only. The observed iPhone Safari failure remains open until this exact HEAD passes physical iPhone Safari, iPhone Chrome, Android Chrome and one alternative Android browser. Playwright WebKit is not device evidence. |
| Real devices / operational restore | separate authorized desktop, iPhone/Safari, iPhone/Chrome, Android/Chrome, alternative Android browser and off-VM decrypt/restore gates | current production iPhone Safari long-waveform smoke failed before this corrective implementation; no corrective device result claimed yet | BLOCKED pending physical-device execution of `s10a-mobile-waveform-harness.html`; no production activation authorized |

## Superseded visual evidence

The PNG files under `tests/safety/evidence/s10a/` document the rejected cross-origin Storage Access implementation from the earlier S10A iteration. They are retained only as historical artifacts and **must not be used as acceptance evidence** for this FIX. Current acceptance evidence is the deterministic same-origin browser suite; it uses synthetic data and never contacts production.

Current Chromium screenshots are under `tests/safety/evidence/s10a-fix/`: landing, Archive and Editor at 320, 390, 768 and 1280 CSS pixels. They were inspected for layout/overflow and contain synthetic empty-archive/local-WAV state only. Login/reconnect password fields, cookies and CSRF values are intentionally not captured.

PR #46 was merged before this follow-up audit. The corrective PR does not perform another activation, deployment, timer installation, real secrets backup, secret rotation, or archive mutation.

## First guarded production attempt (failed before replacement)

This operational record documents the defect that motivated the corrective change; it is not a successful activation or readiness claim.

- Accepted source: `309e9ae1b02866839dbe851a4d142295bea46b3b`.
- Wrapper run count: `1`; exit code: `1`.
- Failure point: rollback-state capture while `replacement_started=false`. The unsafe Docker Go template accessed optional `.State.Health`; the legacy Caddy container had no healthcheck and Docker returned `map has no entry for key "Health"`.
- Candidate images were neither loaded nor activated. Container replacement, authenticated/source smoke, runtime/config installation, deployment record and post-activation recovery bundle were not reached.
- Production remained on gateway `sha256:437a1939e000ca1034f9790cce621a280b3734229f8da69b36ba7ae02416f55a` and Caddy `sha256:5f5c8640aae01df9654968d946d8f1a56c497f1dd5c5cda4cf95ab7c14d58648`; public `/healthz` returned `200`. Rollback was not required because replacement had not started.
- Immutable rollback tags were preserved but not used: `meser-s10a-rollback-20260919-191957-gateway:preserved` and `meser-s10a-rollback-20260919-191957-caddy:preserved`.
- Archive mutations: `0`. Both smoke-input files and temporary plaintext secret staging were removed by wrapper cleanup. The backup timer remains uninstalled and `/etc/fstab` was not changed.
- Sanitized log: `/var/tmp/meser-s10a-activation-309e9ae1-sanitized.log`; SHA-256 `5e0ca9edabc347fb457516e03cdf064dea42b787a9c9f13556df3bb479b0cc03`.
- A public SSH key for user `alex` was installed under separate explicit authorization. No root SSH key or sudoers change was made.

## Second guarded production attempt (readiness incident, rolled back to old images)

This record is diagnostic evidence for the corrective readiness change. It is not a successful activation or authorization to retry.

- Accepted source: `1ff33848bf228d6212f4691804911ef3d0533a57`; tree `f5f966dc297396c0b92b36a81e77aa8376f6f239`.
- Wrapper run count: `1`; exit code `35`; `replacement_started=true`.
- Candidate gateway reached Docker `healthy` at approximately `09:31:20.830Z`; candidate Caddy started at approximately `09:31:21.669Z`.
- The single immediate public probe failed with curl `35` before HAProxy had a healthy Caddy backend. HAProxy recorded the backend DOWN at `09:31:19.834Z`.
- Rollback began immediately. Candidate containers were destroyed; no stopped candidate container or candidate container log remains. Candidate image archives remain available by their accepted exact tags.
- Restored gateway started at approximately `09:31:24.312Z` and became healthy at `09:31:29.442Z`; restored Caddy started at approximately `09:31:30.433Z`. HAProxy marked the restored Caddy backend UP at `09:31:33.852Z`.
- The former rollback loop nevertheless timed out because it derived its probe origin from the captured legacy `runtime.env`; the accepted canonical service origin is `https://meserproject.duckdns.org`. The canonical public `/healthz` subsequently returned HTTP `200` with `.ok=true`.
- Final production images are the exact prior gateway `sha256:437a1939e000ca1034f9790cce621a280b3734229f8da69b36ba7ae02416f55a` and Caddy `sha256:5f5c8640aae01df9654968d946d8f1a56c497f1dd5c5cda4cf95ab7c14d58648`.
- Authentication/source smoke, runtime/config installation, deployment record and post-activation recovery bundle were not reached. Archive mutations remained `0`; wrapper cleanup removed both smoke inputs and plaintext secret staging.
- Sanitized incident log: `/private/tmp/s10a-activation-20260920T093105Z-sanitized.log`; SHA-256 `387ed5a5168f2c39d53c7b7c37b16b4e9ea5f48ea6de0fda3d6d5d69e057295e`.
