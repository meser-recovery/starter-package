# S09B rendered evidence

Source target: `[IMPLEMENTATION_SHA]` from branch `codex/s09b-unified-zoom-recording-ux`.
Fixture: generated audio + real client/gateway/domain over `MemoryRepository`; production gateway and GitHub archive destinations blocked.

The focused browser run covers the following rendered states. PNG filenames are generated by `browser_smoke.py --screenshot-dir tests/safety/evidence` and must be inspected at the implementation SHA before review:

- `overview-{320,390,768,1280}.png` — default all-record list.
- `combined-filters-{320,390,768,1280}.png`, `no-matches-{...}.png` — filters/reset/empty result.
- `detail-{320,390,768,1280}.png` and `archive-intent-detail-390.png` — consolidated detail, project, both histories and deep link.
- `source-deleted-390.png` — deleted sources with retained result access.
- `attention-record-390.png`, `contextual-recovery-390.png`, `maintenance-unassociated-390.png` — attention and recovery separation.
- `deletion-*.png` — contextual version/series/source/purge confirmation states.
- `editor-arrival-{announcement,speaker}-390.png` and `ineligible-*.png` — Editor current record/workspace and ineligible arrivals.
- `s08b-archive-overview-{390,1280}.png` inside this S09B directory — Editor picker (historical filename retained only inside the new evidence directory).
- the additional `s08b-*`, `s08c-*`, `s08d-*` files inside this directory are current S09B executions of preserved Announcement/Speaker interactions; no historical evidence directory is overwritten.

Automated screenshot capture does not replace independent visual/usability ACCEPT. The committed index identifies every generated state; the live review preview remains the approval surface for this PR.
