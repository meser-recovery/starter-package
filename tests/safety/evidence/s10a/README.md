# Superseded S10A screenshots

These PNG files show the earlier cross-origin Storage Access implementation. That architecture was rejected by the S10A FIX and the images are retained only for history. They are not current acceptance evidence.

Current automated evidence comes from `tests/safety/s10a_browser_smoke.py`, using a local same-origin protected-service fixture and synthetic audio. No password, cookie, CSRF value or production archive data is recorded.
