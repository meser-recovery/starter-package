# S09C corrective visual matrix

Binding references, in priority order:

1. `06_53_05.png` — consolidated screen/state and responsive direction.
2. `06_52_52.png` — full-screen Speaker workspace geometry.
3. `06_52_33.png` — Archive list and record detail geometry.
4. `06_50_41.png` — end-to-end screen relationship.

This matrix governs presentation only. Processing, DSP, gateway calls, the archive data model, recovery, versioning, and deletion semantics remain unchanged. Names, avatars, roles, Settings, portal version, legal links, dates, and thumbnails shown in the references are illustrative and do not authorize product functionality.

| Approved preview | Current implementation at `439f6b41d9a00c485a4325be64aaef185ebd9e6d` | Required corrective implementation |
| --- | --- | --- |
| Persistent app shell with a compact dark left navigation and a clear, edge-to-edge work surface | Conventional top website header leaves the audio UI reading as a page inside the broader site | Build an audio-only app shell from the existing Meser identity and existing Editor/Archive destinations. Do not add placeholder account, role, Settings, privacy, or legal functionality. Keep existing logout/service access available through progressive disclosure. |
| Editor entry is a quiet light surface with one heading, two equal source cards, clear blue actions, and one informational strip | Entry is a sequence of large bordered cards with numbered steps and competing status/detail blocks | Make source choice the dominant first screen; reduce chrome, equalize the two choices, and move connection/technical status out of the primary hierarchy. |
| Archive record selection is a focused modal: search, quick filters, compact rows, one primary action | Archive selection expands inline and resembles a general settings/card section | Present the existing selector as a modal over the Editor shell, preserve real filtering and selection behavior, and use compact rows with a constrained viewport. |
| Local-file selection is a focused modal: drop zone, selected-file list, clear primary action | Local selection shares the same inline expansion and its controls are visually dispersed | Present the existing device flow as a modal with a clear drop target, compact selected tracks, and a single confirmation action; preserve validation and file handling. |
| Loading is a centered progress dialog with plain-language steps and one progress bar | Progress/status content remains embedded in the surrounding page, so the transition is visually ambiguous | Surface the existing preparation state in a blocking visual dialog without changing loading, cancellation, retry, or source preparation behavior. |
| Selected record and workflow choice form one light, compact step: record summary first, then two equal mode cards | Current record and workflow cards are separate large sections with excessive vertical distance | Combine their visual hierarchy, compact the record summary, and keep Announcement/Speaker as the only dominant next actions. Saved versions remain behind disclosure. |
| Announcement is a simplified light workspace in the same portal language | Announcement inherits a large dark DAW treatment and exposes many secondary controls at once | Use a light workspace with a compact transport, central multi-track waveforms, and one clear automatic-processing panel. Keep loop, zoom, navigation, diagnostics, source archive, and result/version actions available through progressive disclosure. |
| Speaker is a professional full-screen dark DAW: waveform/timeline dominates, compact track controls at left, one edit-tool panel at right | Speaker is dark but remains a tall stacked card; toolbar, selection controls, waveforms, render, and result compete vertically | Recompose the existing controls into a full-viewport workspace: top project/transport bar, left track headers, central synchronized timeline, consolidated right editing/project panel, and a bottom result region. Preserve all existing control IDs and behavior. |
| Editing tools are one coherent block; exact selection and project save controls sit in the same right rail | Editing actions, exact values, history, zoom, save, and render are separated into several unrelated blocks | Group the existing tools visually into a single right rail with progressive technical/help disclosures. Keep selection scope and disabled-state reasons visible. |
| Result has its own waveform/timeline and player, separate from source playback, with clear archive/download actions | Result exists but reads as another stacked result card below an already long editor | Give result a distinct bottom workspace with its own waveform, transport, metadata, download/archive actions, and adjacent saved-version disclosure. |
| Archive index is a light, dense list with search/filter controls and rows; no editor-style source chooser precedes it | Archive index begins with an introductory chooser and large connection/filter cards before the records | Make the real record list primary, keep search/filter/sort compact, move connection and advanced filters to disclosure, and retain pagination/status behavior. |
| Archive record opens as a separate detail screen with a record header, distinct source/project/result cards, and an Additional section | Detail data is present but visually resembles one long management form with many equally weighted sections | Recompose the same data/actions into a separate detail view, promote sources/projects/results, and put management, technical history, recovery, and dangerous actions under progressive disclosure. |
| Dialogs use one calm visual grammar for save, reconnect, recovery, dependency warning, and deletion | Dialog semantics exist, but density, hierarchy, and destructive emphasis vary across flows | Unify sizing, spacing, title/status hierarchy, primary/secondary actions, warning color, and mobile behavior without changing confirmation or recovery semantics. |
| Mobile examples at 390 px preserve the task surface, keep actions reachable, and turn complex layouts into focused panels | Mobile technically fits, but long stacked cards and dense DAW controls obscure the active task | Collapse the app navigation, use full-width modal sheets, preserve horizontal waveform navigation, stack only secondary panels, and keep primary actions visible at 390 px. |

## Screenshot acceptance set

Corrective evidence must be captured from the real `Audio-Editor.html` and `Audio-Archive.html` preview for: Editor initial, archive selection, local files, loading, workflow choice, Announcement workspace, full-screen Speaker workspace, result, Archive list, Archive record detail, dialogs, and representative 390 px states.
